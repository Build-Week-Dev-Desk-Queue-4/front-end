import React from 'react';
import Sidebar from './Sidebar';
import TicketList from './TicketList';

export default () => {
    return (
        <div>
            <Sidebar />
            <TicketList />
        </div>
    );
}